# Loaded when running playbooks from this directory (cd ansible && ansible-playbook ...).
[defaults]
# Avoid noisy interpreter discovery warnings on macOS/Homebrew Python.
interpreter_python = auto_silent
