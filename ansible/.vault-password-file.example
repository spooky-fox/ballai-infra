# Place your vault password in .vault-password-file (gitignored)
# Or set ANSIBLE_VAULT_PASSWORD_FILE env var
# For CI, use: echo "$VAULT_PASSWORD" > .vault-password-file
REPLACE_WITH_YOUR_VAULT_PASSWORD
