# Copy to backend.tf and customize. Do not commit credentials.
#
# Use a dedicated state key for this root module (separate from Ballai prod).
#
# terraform {
#   backend "s3" {
#     bucket         = "your-org-terraform-state"
#     key            = "lanzo-web/prod/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#   }
# }
