# Copy to backend.tf and customize. Do not commit real credentials.
#
# Example: AWS S3 backend (uncomment and replace placeholders).
#
# terraform {
#   backend "s3" {
#     bucket         = "your-org-terraform-state"
#     key            = "ballai/prod/terraform.tfstate"
#     region         = "us-east-1"
#     encrypt        = true
#     use_lockfile   = true
#     # role_arn     = "arn:aws:iam::ACCOUNT:role/terraform-state"
#   }
# }
