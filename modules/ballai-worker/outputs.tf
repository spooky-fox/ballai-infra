output "summary" {
  description = "Human-readable scaffold marker (no resources yet)."
  value       = "ballai-worker module: scaffold only; account_id=${var.account_id} prefix=${var.name_prefix}"
}
